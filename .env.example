# Optional. Without a token GitHub limits search requests to 60/hour.
# With a personal access token (no special scopes needed for public repo search)
# that goes up to 5,000/hour. Create one at https://github.com/settings/tokens
GITHUB_TOKEN=

# Optional, defaults to 3000
PORT=3000
